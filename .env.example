# Kitsu Frontend Configuration

# Port to expose Kitsu production server on (default: 8080)
KITSU_PORT=8080

# Port to expose Kitsu development server on (default: 8081)
KITSU_DEV_PORT=8081

# Zou Backend Configuration
# These URLs point to your Zou backend instance

# Zou API endpoint (REST API)
KITSU_API_TARGET=http://localhost:5000

# Zou Events endpoint (WebSocket for real-time updates)
KITSU_EVENT_TARGET=http://localhost:5001

# Example for remote Zou instance:
# KITSU_API_TARGET=http://zou.example.com:5000
# KITSU_EVENT_TARGET=http://zou.example.com:5001

# Example for Docker Compose (if Zou is in same network):
# KITSU_API_TARGET=http://zou:5000
# KITSU_EVENT_TARGET=http://zou-events:5001

# Example for Docker on same host:
# KITSU_API_TARGET=http://host.docker.internal:5000
# KITSU_EVENT_TARGET=http://host.docker.internal:5001
